import Vue from 'vue';
import Vuex from 'vuex';

/* language */
import language from './language';

Vue.use(Vuex);

export default new Vuex.Store({
   state: {},
   mutations: {},
   actions: {},
   modules: { language }
});
